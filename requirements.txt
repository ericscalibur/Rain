# Rain - Core dependencies
# Install: .venv/bin/pip install -r requirements.txt

# ── Web server ────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0

# ── Phase 8: Voice (STT) ─────────────────────────────────
# faster-whisper is the recommended backend: small, fast, int8 CPU inference
# Pulls in ctranslate2 + huggingface_hub (downloads 'base' model ~150MB on first use)
faster-whisper>=1.0.0

# ── Phase 8: Voice (TTS) — future ────────────────────────
# piper-tts                 # uncomment when Phase 8 TTS is implemented

# ── MemPalace — ChromaDB-backed semantic memory (Tier 3b) ─
# Spatial palace architecture: Wings → Rooms → Drawers.
# Provides 96.6% R@5 retrieval with metadata filtering.
# Brings chromadb as a transitive dependency.
mempalace>=3.0.0
